name: hankan_chinese_reader
version: 1.0.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-hankan_chinese_reader: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.hankan_chinese_reader # adjust accordingly to your app name

apps:
  hankan_chinese_reader:
    command: hankan_chinese_reader
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-hankan_chinese_reader
parts:
  hankan_chinese_reader:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
